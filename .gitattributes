# Normalise line endings so the repo is consistent across Windows and the
# Linux box that serves the tablets.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.webp binary
*.svg text eol=lf
*.woff2 binary
*.mp4 binary
*.mp3 binary
