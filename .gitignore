# Dependencies
node_modules

# Build output
dist
dist-ssr
release
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Source material: raw camera/audio masters and the reference project
# archive. Working files only - not part of the deliverable.
docs/

# Media package. The tablets' video, audio and image assets are delivered
# as a separate package (see README) rather than through git, so the repo
# stays small enough to clone quickly.
public/media/
